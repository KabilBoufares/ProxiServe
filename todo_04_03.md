📌 **To-Do List : Authentification (Spring Security + JWT) & Sécurité**  
  

---

### **1️⃣ Mise en place de l'authentification avec Spring Security & JWT**  
✅ **done** Configurer Spring Security pour protéger les endpoints.  
✅ **done** Ajouter Spring Boot Starter Security et JWT dans `pom.xml`.  
✅ **done** Créer un modèle `User` avec les rôles (`ROLE_CLIENT`, `ROLE_ARTISAN`, `ROLE_COMPANY`).  
✅ **done** Implémenter le hachage des mots de passe avec BCrypt.  
✅ **done** Créer un service `UserDetailsService` pour charger les utilisateurs.  

---

### **2️⃣ Endpoints d'authentification**  
✅ **done** `POST /api/auth/signup` : Inscription avec vérification de l'email unique.  
✅ **done** `POST /api/auth/login` : Connexion et génération d'un JWT.  
✅ **done** `POST /api/auth/reset-password` : Réinitialisation du mot de passe avec email.  
✅ **done** Créer un `AuthController` pour gérer ces endpoints.  

---

### **3️⃣ Gestion des tokens JWT**  
✅ **done** Créer un filtre JWT (`JwtAuthenticationFilter`) pour valider les requêtes authentifiées.  
✅ **done** Gérer l'expiration et le rafraîchissement des tokens.  
✅ **done** Ajouter un service `JwtTokenProvider` pour générer et valider les tokens.  

---

### **4️⃣ Sécurisation des routes**  
✅ **done** Configurer Spring Security pour exiger JWT sur certains endpoints.  
✅ **done** Protéger les routes `/api/artisans/nearby`, `/api/users/me`.  
✅ **done** Autoriser certaines routes publiques (`/api/auth/signup`, `/api/auth/login`).  

---

### **5️⃣ Sécurité avancée**  
✅ **done** Configuration HTTPS avec certificat auto-signé (développement local).  
⬜ **En attente : Configurer HTTPS via Let's Encrypt en production en cours .**  
⬜ **Limiter les tentatives de connexion pour éviter les attaques bruteforce.**  
✅ **done** Ajouter la gestion des rôles (`ROLE_CLIENT`, `ROLE_ARTISAN`, `ROLE_COMPANY`) dans l'accès aux endpoints.  

---

