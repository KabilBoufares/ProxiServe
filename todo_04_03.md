### **📌 To-Do List : Authentification (Spring Security + JWT) & Sécurité**  

#### **1️⃣ Mise en place de l'authentification avec Spring Security & JWT**  
- [ ] **Configurer Spring Security** pour protéger les endpoints.  
- [ ] **Ajouter Spring Boot Starter Security et JWT** dans `pom.xml`.  
- [ ] **Créer un modèle `User` avec les rôles (`ROLE_CLIENT`, `ROLE_ARTISAN`, `ROLE_COMPANY`)**.  
- [ ] **Implémenter le hachage des mots de passe avec BCrypt**.  
- [ ] **Créer un service `UserDetailsService` pour charger les utilisateurs**.  

#### **2️⃣ Endpoints d'authentification**  
- [ ] **POST `/api/auth/signup`** : Inscription avec vérification de l'email unique.  
- [ ] **POST `/api/auth/login`** : Connexion et génération d'un JWT.  
- [ ] **POST `/api/auth/reset-password`** : Réinitialisation du mot de passe.  
- [ ] **Créer un `AuthController` pour gérer ces endpoints**.  

#### **3️⃣ Gestion des tokens JWT**  
- [ ] **Créer un filtre JWT (`JwtAuthenticationFilter`) pour valider les requêtes authentifiées**.  
- [ ] **Gérer l'expiration et le rafraîchissement des tokens**.  
- [ ] **Ajouter un service `JwtTokenProvider` pour générer et valider les tokens**.  

#### **4️⃣ Sécurisation des routes**  
- [ ] **Configurer Spring Security pour exiger JWT sur certains endpoints**.  
- [ ] **Protéger les routes `/api/artisans/nearby`, `/api/users/me`**.  
- [ ] **Autoriser certaines routes publiques (`/api/auth/signup`, `/api/auth/login`)**.  

#### **5️⃣ Sécurité avancée**  
- [ ] **Configurer HTTPS via Let's Encrypt (en production uniquement)**.  
- [ ] **Limiter les tentatives de connexion pour éviter les attaques bruteforce**.  
- [ ] **Ajouter la gestion des rôles (`ROLE_CLIENT`, `ROLE_ARTISAN`, `ROLE_COMPANY`) dans l'accès aux endpoints**.  

---
